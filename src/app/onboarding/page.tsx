import OnboardingEntry from "./OnboardingEntry";

export const metadata = { title: "Choose your username — PrivyPay", robots: { index: false } };

export default function OnboardingPage() {
  return <OnboardingEntry />;
}
